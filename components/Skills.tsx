import { skillGroups } from "@/data/content";
export function Skills() { return <section className="section"><p className="eyebrow">02 — Expertise</p><h2 className="section-title">From insight to impact.</h2><div className="mt-12 grid gap-x-14 gap-y-10 md:grid-cols-2">{skillGroups.map((group) => <div key={group.title} className="border-t border-ink/15 pt-5"><h3 className="font-semibold text-ink">{group.title}</h3><div className="mt-4 flex flex-wrap gap-2">{group.skills.map((skill) => <span key={skill} className="rounded-full bg-mist px-3 py-1.5 text-sm text-ink/75">{skill}</span>)}</div></div>)}</div></section>; }
